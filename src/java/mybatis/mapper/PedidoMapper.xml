<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="pedido">
    
    <select id= "obtenerPedidos" resultType="modelo.pojo.Pedido">
        SELECT * FROM pedidos
    </select>
    
    <select id="obtenerEstados" resultType="modelo.pojo.Estado">
        SELECT * FROM estado
    </select>
    
    <select id="obtenerPedidoPorId" parameterType="int" resultType="modelo.pojo.Pedido">
        SELECT * FROM pedidos WHERE idPedido = #{idPedido}
    </select>
    
    <insert id = "registrarPedido" parameterType="modelo.pojo.DatosRegistroPedido" statementType="CALLABLE">
        INSERT INTO (fechaPedido, fechaEntrega, nombreCliente, telefono, abono, total, descripcion, fotografia, id_formaPago, id_estado) 
        VALUES (#{fechaPedido}, #{fehcaEntrega}, #{nombreCliente}, #{telefono}, #{abono}, #{total}, #{descripcion}, #{fotografia}, #{id_formaPago}, #{id_estado}) 
    </insert>
    
    

    <delete id="eliminarPedido">
        
    </delete>
    
</mapper>
